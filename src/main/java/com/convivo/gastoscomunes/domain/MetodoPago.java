package com.convivo.gastoscomunes.domain;

/** Método utilizado para pagar (total o parcialmente) un gasto común. */
public enum MetodoPago {
    TRANSFERENCIA,
    TARJETA,
    EFECTIVO,
    OTRO,
}
